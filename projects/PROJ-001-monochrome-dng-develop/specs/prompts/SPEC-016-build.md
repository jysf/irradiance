# SPEC-016 — build dispatch

`HANDOFF-038` is the contract. All five findings are measured at design; the
build reproduces numbers rather than discovering them. Every test is tier A on
purpose — each finding is about a surface that lies when something is absent,
and CI is where things are absent.
