# PATCH-003 — round 2 re-verify dispatch

`HANDOFF-041` is the contract. Third round on one gate, all orchestrator-authored.
The last two rounds were wrong the same way: an over-claim about what a scan could
reach, defended by a proof that modelled one shape. The prior is that shape three
exists.
