# SPEC-015 — build dispatch

Hand the fenced block to a fresh CLI session in this repo. `HANDOFF-035` is the
contract; the design probe already ran, so this build reproduces measured
numbers rather than discovering them.
