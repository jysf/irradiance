# PATCH-002 — verify dispatch

`HANDOFF-039` is the contract. The orchestrator wrote this patch, so this verify
is doing the job a separate build session normally would. Attack the red-proof
first — one false version of it already shipped and was caught by mutation.
