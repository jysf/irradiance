# PATCH-003 — verify dispatch

`HANDOFF-040` is the contract. This is the orchestrator's remediation of the
orchestrator's own ship-blockers, so the reviewer's independence is the only
independence in the chain. PATCH-002's verify found its best defect (`M4b`) by
attacking the PROOF rather than the code — do that again.
