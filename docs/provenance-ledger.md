# Provenance ledger

**Every algorithm and every decoder in `irradiance` gets a row here, recording
where it came from and under what licence.**

This exists because the licence a crate *declares* and the provenance its code
*carries* are different things, and `cargo deny` only sees the former. The
motivating example is real: `demosaic` 0.3.0 ships `MIT OR Apache-2.0`, and its
`markesteijn_impl.rs:11` says *"Ported from LibRaw's `xtrans_interpolate(1)`"* —
LibRaw being LGPL-2.1/CDDL. That crate passes a licence gate green while carrying
a self-documented port of copyleft code.

The ledger is also the thing that makes this library's permissive claim
*defensible* rather than merely asserted. No other RAW project in any language
has one.

## The rule

Ranked best to worst. Prefer the highest available:

1. **A published specification** — zero contamination. The Adobe DNG Specification
   is public and carries a patent grant for compliant implementations.
2. **A published paper** — the algorithm, not someone's code. Malvar-He-Cutler
   (ICASSP 2004) prints its kernels; Hirakawa & Parks likewise.
3. **Our own prior code** — e.g. crustyimg, same author.
4. **Reading a permissively-licensed implementation** — allowed, must be recorded,
   and the source licence's terms must be honoured.
5. **Reading a copyleft implementation** — **not permitted.** If an algorithm is
   only available this way, it does not ship in the default build; it needs its
   own decision, and probably an off-by-default feature.

"I read it years ago and reimplemented from memory" is row 5, not row 3. Record it
honestly.

## The ledger

| Module / algorithm | Source | Source licence | Provenance class | Notes |
|---|---|---|---|---|
| `src/ifd.rs` — TIFF/IFD container walk, SubIFD recursion, sensor-IFD selection, typed tag extraction | TIFF 6.0 (1992) §2 "Image File Header" + §2 "Image File Directory"; Adobe DNG Specification 1.7.1.0 (DNG-private tags 50713/50714/50717/50719/50720/50829/51008/51009/51022, `PhotometricInterpretation = LinearRaw` 34892) | public specifications; DNG carries a patent grant for compliant implementations | 1 — specification | **SPEC-003.** Written from the specs, not from any implementation. `dnglab`/`rawler` (LGPL-2.1) were **run as tools** to produce the ground truth this reader is checked against, and their source was not read — running imposes nothing, reading would be a `provenance-recorded-per-algorithm` violation. `SPIKE-001`'s decoder was discarded unmerged and deliberately not consulted (`test-before-implementation`). Tag numbers and field types were re-derived by reading the real files' bytes; the expected values are cross-checked against `exiftool 13.55` on all 7 corpus files in `tests/ifd_reader.rs`. Implement from the **spec**, not the DNG SDK: the SDK's terms are ambiguous and its patent licence reportedly does not cover it. **SPEC-004** extended this same row's scope, same class: `ActiveArea`/`DefaultCropOrigin`/`DefaultCropSize` as named-field structs (DNG 1.7 §Chapter 4's own field order, no new tags), and FU-11's fix to sensor-IFD selection's error handling (`DEC-012`) — a malformed identifying tag on one IFD no longer aborts the scan of the others. No implementation was consulted for either; both are control-flow/typing refinements of the existing spec-derived rule, re-verified against `exiftool` and re-fuzzed. **SPEC-007** extended this same row's scope, same class, twice: (1) `TYPE_RATIONAL` — TIFF 6.0 §2 "Types" defines it as two `LONG`s, numerator then denominator, read directly from the spec text; a zero denominator or non-integral ratio is `Error::MalformedRationalValue`. (2) `DEC-012`'s amended structure/interpretation split applied to `sensor()` itself (FU-16, FU-17) and to `is_sensor_ifd`'s tag-evaluation order (FU-20) — control-flow refinements of the existing spec-derived rule, not a new algorithm. No implementation was consulted; re-verified against `exiftool` on all 7 corpus files and re-fuzzed (11,553,927 runs, zero crashes). **SPEC-008** extended this same row's scope, same class, three times, all pinning `DEC-012` rather than redrawing it: (1) `uints()`'s `TYPE_RATIONAL` acceptance made **per-tag** (`is_structural_tag`) instead of global (`SPEC-007/FU-4`) — TIFF 6.0 defines every `DEC-012` Structure tag as BYTE/SHORT/LONG only, so RATIONAL is illegal for all of them; this restores `main`'s per-tag behaviour for exactly `DEC-012`'s amended Structure row, while interpretation tags keep the widening `SPEC-007` added. (2) `sensor()`'s `Orientation` read fixed to cost the field **at most once** and only when no valid value was found anywhere (`SPEC-007/FU-1,FU-2`) — a control-flow correction to the existing IFD0-then-sensor-IFD fallback, not a new rule. (3) Four new hand-built fixtures pin `Compression`/`StripOffsets`/`StripByteCounts`/`BitsPerSample` as fatal-when-malformed, alongside the existing `RowsPerStrip` fixture — no new tags, no new read logic. No implementation was consulted for any of the three; all six mutants (the four structural tags, the `SubIFDs` RATIONAL gate, the `Orientation` fallback) were verified to flip the corresponding test red when reverted and green when restored, and the full suite was re-fuzzed (12,971,280 runs, zero crashes). **SPEC-009** extended this same row's scope, same class, adding **zero** new tags and **zero** src/ read logic: it is the table-driven test `SPEC-008` measured missing — `is_structural_tag()` named eleven tags with exactly one (`TAG_SUB_IFDS`) enforced by any test — plus `DEC-015`, which decides `SPEC-008/FU-3` (a well-formed `IFD0` `Orientation` with an erroring sensor-IFD read stays silent in `malformed_tags`, narrowing that field's doc comment to match) without changing `sensor()`'s existing behaviour. All eleven mutations (each membership deleted from `is_structural_tag()` in turn) were watched to fail, and the unmutated tree watched to pass, as the control; the full suite was re-fuzzed (13,541,962 runs, zero crashes) |
| `tests/support/corpus.rs::sha256` — SHA-256 | FIPS PUB 180-4 §§4.2.2, 5.1.1, 5.3.3, 6.2.2 | public standard (US Government work, no licence restriction) | 1 — specification | **Dev-only test support; not in the library.** Written from the published standard, not from any implementation. Verifying corpus files against the `sha256` DEC-003 pins. Proven against the published NIST vectors (`""`, `"abc"`, the 448-bit vector, 10⁶ × `'a'`) in `tests/corpus_manifest.rs`, and cross-checked against all 7 real corpus files whose digests were recorded independently (raw.pixls.us' own DB and `shasum`). A file-integrity check, not a security boundary. See DEC-010 for why this is not a dependency |
| `tests/support/md5.rs` — MD5 | RFC 1321 §§3.1, 3.3, 3.4, 3.5 | public standard (IETF RFC, no licence restriction) | 1 — specification | **SPEC-013.** Dev-only test support; not in the library. Written from the published RFC, not from any implementation — same shape as `sha256` above (`DEC-010`'s reasoning extends unchanged: a hashing crate would only be exercised on a machine holding the corpus, invisible to CI). Proven against all seven of RFC 1321 Appendix A.5's own published test vectors (`md5_matches_the_rfc_1321_test_vectors`, `tests/plane_oracle.rs`, tier A). Also embedded verbatim (`include_str!`) into the plane oracle's red-proof probe binary (`DEC-017`) so the mutated-crate build and the test binary hash with the exact same implementation. Used to check our unpacked plane against `dnglab analyze --raw-checksum`'s pinned digest (`docs/oracle-contract.md`) — a file/output-integrity check, not a security boundary |
| `src/plane.rs` — sensor-plane strip location and sample unpack, `DEC-008`'s two-path rule (MSB-first sub-byte bit stream vs. byte-aligned integers) | TIFF 6.0 (1992) §2 "Compression" (`= 1`, uncompressed); `DEC-008`, itself derived from TIFF 6.0's packing rule | public specification | 1 — specification | **SPEC-012.** The first spec that produces pixels rather than tags. `SPIKE-001`'s decoder is discarded and was **not consulted** (`test-before-implementation`, `provenance-recorded-per-algorithm`) — the bit-cursor arithmetic was re-derived from `DEC-008`'s byte evidence and hand-verified against the measured strip bytes before being written (worked by hand for samples 0-1 of `L1021223.DNG`: `0b a8 2d 50…` MSB-first at 14 bits gives 746, 725, matching `dnglab --raw-pixel`'s own plane exactly). Cross-checked end-to-end against **two independent real files, both bit depths**: `L1021223.DNG` (14-bit, sub-byte path) and `L1000622.DNG` (16-bit, byte-aligned path) — `tests/plane_unpack.rs`'s `unpacks_fourteen_bit_msb_first_samples`/`unpacks_sixteen_bit_in_file_byte_order`, and manually via `irr unpack`. The three compressed corpus files (`M2462362.DNG`, `K3III.DNG`, `K3III.PEF`) are rejected before any strip read (`Sensor::require_uncompressed`), never decoded. No implementation was consulted; fuzzed 13,050,886 runs (45s) reaching both paths via seeds in `fuzz/seeds/plane/`, zero crashes |
| `src/develop.rs` — level normalization (`BlackLevel`/`WhiteLevel` affine map, clamped, `DEC-018`) and the three-stage `ActiveArea` → `DefaultCrop` (`DEC-019`) → `Orientation` geometry | DNG 1.7 §Chapter 4 (`BlackLevel`, `WhiteLevel`, `ActiveArea`, `DefaultCropOrigin`/`Size`) for the affine map and crop; TIFF 6.0 tag 274 `Orientation`'s eight-value semantics, cross-checked against `exiftool`'s own labels already recorded in `docs/measured-q2m-dng.md` rather than transcribed from an unverifiable page number | public specification (DNG); public specification, evidenced against a live tool (`Orientation`) | 1 — specification | **SPEC-014.** This spec has **no oracle** (`DEC-004`): `dnglab --raw-checksum` attaches before any of this, and the develop oracle (SSIMULACRA2) is blind to a levels error up to +256 (50%). Every arithmetic claim is therefore asserted analytically, against tag values read from the file, in `tests/develop.rs` and `src/develop.rs`'s own unit tests — no comparison oracle exists to cross-check against until `SPEC-015`. The `Orientation` eight-value row/column table was hand-derived from its own stated semantics (not transcribed from any implementation) and independently verified against a worked 2x3 example for all eight values (`crop_source_coords_matches_the_worked_example_for_all_eight_orientations`). `DEC-019`'s `ActiveArea`-relative crop origin is corroborated by `SPEC-005`'s independent `dnglab`/`exiftool` cross-check (`tests/metadata_oracle.rs::dnglab_crop_origin_is_active_area_plus_default_crop_origin`), since no decodable corpus file has a non-zero `ActiveArea` origin to test it directly — `AC4`'s hand-built fixture is what actually exercises the reading. No implementation was consulted; fuzzed 14,562,321 runs (61s) via seeds in `fuzz/seeds/develop/`, zero crashes. **SPEC-018** extended this row, same class: `develop_into` gained a `WarpRectilinear` application stage between levels normalization and `DefaultCrop`/`Orientation` (see `src/opcode.rs`/`src/warp.rs`'s own rows below for the warp itself). Design-time re-reading of DNG 1.7 §Chapter 4's own `DefaultCropOrigin`/`Size` text found the ORIGINAL pipeline-order assumption wrong — the warp runs over the full `ActiveArea` window, before `DefaultCrop` extracts the smaller final rectangle, not after (`DEC-024` Finding 1) — corrected via two new helpers, `normalize_active_area_into`/`crop_and_orient_from_active_into`, used only when a real warp is present; the original single-pass, zero-extra-allocation `crop_orient_normalize_into` is unchanged and still the only path for warp-free files. No implementation was consulted. **SPEC-017** added `apply_fix_bad_pixels_constant` (a 3x3 median-of-valid-neighbours in-place kernel) to this module, called from `develop_into` before the crop/orient stages when `OpcodeList1` carries a `FixBadPixelsConstant`; its provenance — class 1, hand-written from the DNG opcode's own words, no implementation consulted — is recorded in full in the `src/opcode.rs` row below (filed with the opcode's parser rather than duplicated here — `SPEC-017/FU-5`). SPEC-017's added full-raw-plane copy is additive to SPEC-018's warp buffers: measured peak RSS is **559,939,584 bytes** (`/usr/bin/time -l target/release/irr develop L1021223.DNG`, SPEC-017 ship) on a frame carrying both `OpcodeList1` and a non-identity `OpcodeList3` warp — above the `src/warp.rs` row's warp-only figure, which is not superseded but is no longer the whole story for such a frame (`SPEC-017/FU-6`) |
| `src/opcode.rs` — `OpcodeList` byte-stream parser (count/id/version/flags/size framing, big-endian regardless of file byte order) and `WarpRectilinear`'s own parameter layout (`N`, six coefficients per set, trailing `cx_hat`/`cy_hat`) | DNG 1.7.0.0 Chapter 7 "Opcode List Processing" (opcode framing, `Flags` bit 0 optional/mandatory semantics) and §6.4.1 "WarpRectilinear" (parameter layout) | public specification; DNG carries a patent grant for compliant implementations | 1 — specification | **SPEC-018.** Written from the published DNG 1.7.0.0 PDF (`helpx.adobe.com/.../DNG_Spec_1_7_0_0.pdf`) fetched and read directly during design, not from `dnglab`/`rawler` (LGPL-2.1) or any other implementation. Byte-for-byte round-tripped against all three decodable Q2M frames' real `OpcodeList3` bytes (`exiftool -b -OpcodeList3`, committed as `tests/oracle-fixtures/opcodelist3-*.hex`) — the parsed `WarpRect` re-encodes to the identical bytes read from each file (`tests/opcode.rs::opcode_list_3_round_trips_the_q2m_warp`). Confirmed at the byte level: `OpcodeID = 1`, DNG version `1.4.0.0`, `Flags = 0` (mandatory) on all three frames, matching `SPIKE-001`'s claim independently. **Correction to a design-time citation:** `SPEC-018`'s own `## Context` named `OpcodeList3`'s IFD tag as `0xC740` — that is actually `OpcodeList1`'s tag; the correct value, `51022`/`0xC74E`, was already correctly present as `TAG_OPCODE_LIST_3` in `src/ifd.rs` (`SPEC-004`), confirmed against DNG 1.7.0.0 p.56-57 (`AGENTS.md` §16 rule 4, `unrun-docs-carry-errors`). No implementation was consulted; fuzzed 20,077,163 runs (61s) via seeds in `fuzz/seeds/warp_opcode/`, zero crashes. **SPEC-017** extended this row, same class: `Opcode::FixBadPixelsConstant` (`OpcodeID` 4) and its parser branch, parameter layout `Constant:u32be, BayerPhase:u32be` (an exact 8 bytes, no variable-length tail). Byte-for-byte round-tripped against all three decodable Q2M frames' real `OpcodeList1` bytes (`exiftool -b -OpcodeList1`, independently re-verified against all three frames during this build, byte-identical, committed as `tests/oracle-fixtures/opcodelist1-q2m.hex`) — `tests/opcode.rs::parse_opcode_list_reads_the_q2m_fixbadpixels_bytes`. **Correction to a design-time citation:** `SPEC-017`'s own handoff (`HANDOFF-044`) named this opcode's chapter as "DNG 1.7.0.0 § Chapter 6" — Chapter 6 is "Mapping Camera Color Space to CIE XYZ Space", unrelated; `FixBadPixelsConstant`, like `WarpRectilinear` above, is documented in Chapter 7 "Opcode List Processing" (confirmed by fetching the published DNG 1.6.0.0 PDF directly during this build and reading its own table of contents and the `FixBadPixelsConstant` page — `AGENTS.md` §16 rule 4, `unrun-docs-carry-errors`; see `src/opcode.rs`'s own module doc for the full citation). **The interpolation kernel is NOT from the DNG spec** — the spec's own words for this opcode are "patches (interpolates over) bad pixels... marked... by setting the bad pixels to a value of Constant", with no kernel specified. `src/develop.rs::apply_fix_bad_pixels_constant`'s 3x3 median-of-valid-neighbours (SPEC-017's own pre-registered `## Notes for the Implementer`, deterministic lower-of-two-middles tie-break) is this build's own hand-written algorithm, provenance class 1 as a public-domain technique (median filtering, standard, no single canonical paper or implementation source — same class-1-as-technique reasoning `src/warp.rs`'s bilinear kernel row above uses), not read from any implementation. No implementation was consulted for either the parser extension or the kernel; fuzzed 13,696,114 runs (61s) via seeds in `fuzz/seeds/opcode/`, zero crashes. **Measured, not assumed** (`SPEC-017/SB-1`): the applier is proven correct and reachable (`AC1`, `AC4`, `AC8` all pass), but all three decodable Q2M frames measure **zero** raw samples equal to `Constant = 0` — see `tests/develop.rs`'s own `q2m_frames_have_at_least_one_replaced_pixel` (`#[ignore]`d with the measured reason) and this spec's handback for the full finding |
| `src/warp.rs` — `WarpRectilinear` application: the inverse (destination→source) coordinate map and bilinear resampling kernel | Transform: DNG 1.7.0.0 §6.4.1's own stated formula (`cx`/`cy`/`m`/`r`/`f`/`x'`/`y'`). Kernel: bilinear interpolation, a standard, publicly-documented image-resampling technique with no single canonical paper or implementation source (textbook mathematics, predating any RAW-decoder implementation) | public specification (transform); public-domain technique (kernel) | 1 — specification (transform); 1 — public-domain technique (kernel, not read from any crate or paper) | **SPEC-018.** The transform is a direct transcription of DNG 1.7.0.0 §6.4.1's formula, re-derived independently (never from `dnglab`/`rawler`, LGPL-2.1, run as a tool only) and expressed without ever calling `sqrt` (the radial polynomial is entirely in `r^2`). Verified against `SPIKE-001`'s measured ~504 px corner displacement on `L1021223.DNG`'s own coefficients (`tests/warp.rs::warp_moves_corner_impulse_to_spike_001s_source_coord`, `AC4`) and against the other two decodable frames' own coefficients (437.7 px, 407.0 px) — none hardcoded. Bilinear is the pre-registered default kernel (`## The design decision this spec rests on`); the kernel-choice DEC (`DEC-024`) records why no upgrade to bicubic/Lanczos-3 was pursued: `SPEC-020`'s oracle cannot validate this feature in any kernel (`dnglab`/`rawler` do not implement DNG `OpcodeList` processing — verified by inspecting `github.com/dnglab/dnglab`'s source for opcode-application code, finding none), so a fancier kernel would not close the gap a broken oracle reports. Out-of-extent handling (clamp-to-edge) is this build's own choice, DNG 1.7.0.0 §6.4.1 being silent on it (confirmed by full-text search of the specification). No implementation was consulted for either the transform or the kernel; `AC12`'s determinism (`warp_output_is_bit_identical_across_two_runs`) and `AC5`'s identity short-circuit (`identity_warp_is_the_identity_transform`) are both unit-tested directly. Peak RSS on `L1021223.DNG`, measured via `/usr/bin/time -l target/release/irr develop` (`DEC-018`'s Consequences, re-measured post-`SPEC-018`): **465,010,688 bytes** — up from `SPEC-014`'s 275,890,176, the added cost being the two internal `ActiveArea`-sized scratch buffers `develop_into` now allocates for any real (non-identity) warp, which every real Q2M frame carries |
| `tests/develop_oracle.rs` + `tests/support/oracle.rs` — the analytic levels/geometry oracle: the exact real-valued `BlackLevel`/`WhiteLevel` affine map, the `ActiveArea` → `DefaultCrop` rectangle (no `Orientation`), and the permutation/injectivity properties `develop_into`'s output must satisfy | DNG 1.7 §Chapter 4 (`BlackLevel`, `WhiteLevel`, `ActiveArea`, `DefaultCropOrigin`/`Size`) for the affine map and crop-window rectangle | public specification (DNG) | 1 — specification | **SPEC-015.** A SEPARATE implementation from `src/develop.rs`'s row above, deliberately — the ledger tracks implementations, not features, and an oracle sharing a row with the code it checks would be a mirror by construction. Written directly from DNG 1.7's own stated formula and defaults, never from reading `src/develop.rs` (`src/develop.rs`, `src/plane.rs`, `src/ifd.rs` are `0` lines changed vs `main` — `AC7`). **`Orientation`'s eight-case table is never written here in any form** (`SPEC-015`'s central decision, `DEC-020`): AC1/AC2's per-pixel bound and AC3's histogram property are both computed via rank/frequency correspondence over the crop window taken in raster order, which is provably equivalent to the true positional pairing for a monotonic map under a pure permutation, without ever stating what the permutation is. Cross-checked against all three decodable files: max deviation from the exact affine map 0.499968/0.499968/0.499969 (AC1), truncation disagreement 50.1%/49.1%/45.0% (AC2), histogram equality exact on all three including the `Orientation` 6 frame (AC3), distinct levels 15872/16164 matching the full in-range domain exactly (AC4) — every number reproducing the design probe's own measurements exactly. Two red-proofs, one per fault class (`DEC-021`): the `BlackLevel + 64` fault is injected in-process (no source mutation — the field is public); the orientation-identity fault (`SPEC-014/FU-3`'s historical bug) uses `DEC-017`'s mutate-copy-rebuild-run mechanism on a hand-built fixture, no corpus needed (`AC6`). No implementation was consulted; no fuzz target (`SPEC-015`'s Non-Goals) — this file adds no parser and no new input surface |
| `tests/support/ssimulacra2.rs` — the develop-layer perceptual oracle's metric wrapper: 16-bit grayscale → `ssimulacra2::Rgb` conversion (`sample as f32 / 65535.0`, replicated into three channels, sRGB transfer + BT.709 primaries) and the `compute_frame_ssimulacra2` call | rust-av/`ssimulacra2` v0.5.1 (`BSD-2-Clause`), which itself implements the SSIMULACRA2 algorithm published by Cloudinary / Jyrki Alakuijala et al. | permissive third-party crate (`BSD-2-Clause`); full resolved dependency graph confirmed permissive (`MIT`/`Apache-2.0`/`BSD-2-Clause`/`Unicode-3.0`) by `cargo deny check licenses` in a probe workspace, `DEC-023` | 2 — third-party permissive crate | **SPEC-020.** Not class 1: we did not implement SSIMULACRA2 from the published algorithm. Not class 3: we are not reimplementing from another implementation — the crate itself IS the implementation, taken as a dependency. Dev-only, no library-graph impact (`DEC-023`; `src/`, `src/plane.rs`, `src/ifd.rs`, `src/develop.rs` are `0` lines changed vs `main` — `AC10`). The 16-bit→`Rgb` conversion route (`tests/support/pnm.rs` for the reader, `tests/support/perturb.rs` for the tier-A fault generators) is original code following `SPEC-020`'s own pre-specified chain, not read from crustyimg's `src/quality/mod.rs::to_ss_rgb` beyond the reference API call shape (crustyimg decodes via the forbidden `::image` crate, which this repo's `library-not-application` constraint rules out even test-side — the conversion here is a straight 16-bit → float rescale, no decode step). The tier-A red-proof perturbs a synthetic fixture rather than a real render — no develop pipeline exists yet (`SPEC-018`/`SPEC-019` ship it) — and measures: identical 100.000 (`AC3`, ≥ 99.9), 1-pixel shift 61.823 (`AC4`, < 85, caught), missing radial warp (`SPIKE-001` coefficients) −82.338 (`AC5`, < 85, caught emphatically), gamma 1.05 in the linear domain 90.038 (`AC6`, ≥ 85, admitted) — all four reproducing `DEC-005`'s real-photograph calibration table's shape at full 1024x768 resolution (`tests/oracle-fixtures/perceptual-oracle-fixture.md`). No implementation was consulted for the conversion or fault-generator code; no fuzz target (`SPEC-020`'s Non-Goals — this file adds no library-side input surface, only test-side synthetic/dnglab-output parsing) |

## ⚠ The distinction runs BOTH ways — measured 2026-08-20

The motivating example above is a crate that **declares better than it carries**
(`demosaic` ships MIT/Apache over a self-described LGPL port). `libfuzzer-sys`
runs the **inverse**, and the ledger's framing assumed that could not happen:

| | declares | actually carries |
|---|---|---|
| `demosaic` 0.3.0 | `MIT OR Apache-2.0` | a port of LGPL LibRaw code |
| `libfuzzer-sys` 0.4.13 | `(MIT OR Apache-2.0) AND NCSA` | **`Apache-2.0 WITH LLVM-exception`, in all 55 vendored files** |

Counted, not sampled: 49 files at the vendored top level, 55 including
`afl/` and `+dataflow/`, **every one** carrying the LLVM Apache header, and
**0 of 56** mentioning NCSA anywhere. The crate's SPDX expression *and* its own
README are both stale against its code.

Nothing is wrong here — everything involved is permissive, and `deny.toml` enforces
the **stricter** declared reading via a named per-crate exception rather than
widening `allow`. But it means the ledger's question is not "is the declaration too
generous?" It is **"does the declaration match the code, in either direction?"**
A stricter-than-reality declaration is harmless for compliance and still a fact the
ledger should carry, because the next reader will otherwise re-derive it.

Reached only by `just deny-fuzz`; `libfuzzer-sys` is not in the library's graph.

## Standing decisions

- **`demosaic` is a dev-time oracle only, never a dependency** — see above.
- **`rawler`/`rawloader` are never linked**, not even as dev-dependencies, without
  a decision. `dnglab` is used as a *tool*, which imposes nothing.
- **Patents:** the 20-year term puts MHC (2004) and AHD (2005) at or past expiry,
  VNG (1999) long expired, and the Bayer CFA patent (1976) expired ~1993. The DNG
  container carries an explicit grant. Confirm before relying on any of this.

## This library's own licence

**`MIT OR Apache-2.0`** — the Rust ecosystem's dual-licence convention, and
crustyimg's. Apache-2.0 alone would be more restrictive than the norm and would
undercut the entire pitch: permissive *is* the differentiator, and the whole
reason this library exists is that every mature alternative in every language is
copyleft or C++.

`LICENSE-MIT` and `LICENSE-APACHE` both live at the repo root.
